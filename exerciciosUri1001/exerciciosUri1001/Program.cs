// See https://aka.ms/new-console-template for more information

using System.Globalization;

int A, B, calculo1;
A = int.Parse(Console.ReadLine());
B = int.Parse(Console.ReadLine());

calculo1 = A + B;
Console.WriteLine("X = " + calculo1);
